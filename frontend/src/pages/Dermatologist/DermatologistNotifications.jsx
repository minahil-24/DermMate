import NotificationInboxPage from '../../components/common/NotificationInboxPage'

const DermatologistNotifications = () => (
  <NotificationInboxPage
    breadcrumbItems={[{ label: 'Notifications' }]}
    subtitle="Stay updated with verification updates and other alerts"
  />
)

export default DermatologistNotifications
